# Track 2 — Agent core & consequence-gate (brain & conscience)

Intent extraction (LeMUR / ask.sh) -> structured action. Consequence-gate classifies each action; confirm-before-act loop on high-consequence.
Deliverable: given an intent, returns 'acted + receipt' or 'awaiting confirmation + spoken text'.
